# Enable WebGPU unstable APIs in web-sys for WASM builds
# Set smaller initial memory to avoid OOM on memory-constrained systems
[target.wasm32-unknown-unknown]
rustflags = [
    "--cfg=web_sys_unstable_apis",
    "-C", "link-arg=-zstack-size=1048576",
]
