# O2EM controller boundary

O2EM is an inactive legacy SourceForge project. Its published project identity
does not provide a currently pinned, inspectable native configuration grammar,
stable physical-device selector, save-state contract, or firmware checksum
set. The adapter refuses to invent controller serialization; runtime and
platform behavior remain unverified.
