# NetherSX2 controller boundary

Pinned upstream patch/distribution source: `Trixarian/NetherSX2-classic@98ddad36d6c367a045b65fd06fdb26ba4c611387`.

The pinned `README.md` identifies the project as a modified AetherSX2 copy and links Android APKs/install steps (lines 6, 18-26). No official Linux, Flatpak, Windows, or macOS package or portable controller-profile contract is established by the pinned source. The adapter therefore refuses desktop serialization. Android preference/storage, JNI, and input-device behavior require a native Android implementation and runtime verification.
