# Legacy Lunchbox feature parity ledger

The `legacy/electron` branch is the behavioral source for Lunchbox-owned
features. The native port does not embed its web frontend or HTTP server, but it
must retain the useful behavior behind them. This ledger is checked whenever a
native vertical slice is completed so that features are not silently lost.

## Catalog and collection experience

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Full discoverable catalog separated from writable user state | `backend/src/state.rs`, `backend/src/api.rs` | Implemented as read-only catalog layers |
| Platform sidebar, counts, search, grid/list views, zoom and artwork choice | `src/components/sidebar.rs`, `toolbar.rs`, `game_grid.rs` | Implemented with virtualized native views, persisted 50–200% card zoom, and persisted box front/back/3D, gameplay, title-screen, fan-art, or clear-logo choice |
| Installed, non-retail and adult filters | `src/components/toolbar.rs`, `backend/src/api.rs` | Implemented as composable native filters, including an explicit Minerva-covered/not-installed choice and persisted non-retail/adult preferences |
| Regional/version variants and alternate titles | `backend/src/api.rs`, `backend/src/db/schema.rs` | Minerva candidates now parse and display combined regions and revisions, strip those tags for title comparison, normalize common Roman-numeral sequels, and apply the complete persisted region order plus latest/original revision preference without overriding a stronger title match. Catalog-level variant grouping and alternate-title browsing remain in the ledger. |
| Favorites, playlists, normal collections and smart collections | `backend/migrations`, `src/components/sidebar.rs` | Favorites and both manual and smart collections are native and durable by stable game UUID. Manual collections preserve explicit member order and support per-game, reorder, and confirmed bulk visible-result changes. Smart shelves compose title, exact platform, installed/Minerva/catalog availability, favorite, completion, and retail/non-retail/adult rules and refresh when their source state changes. The polished two-pane manager, sidebar navigation, search/platform composition, asynchronous writes, optimistic rollback, restart recovery, and bounded portable JSON import/export are native. Portable matching accepts the exact Lunchbox UUID or an unambiguous positive LaunchBox database ID only; display titles never establish identity. Richer tags and playlist-specific presentation remain in the ledger. |
| Play count, play time, last played, completion state and sessions | `backend/migrations`, `backend/src/handlers.rs` | Native and durable by stable game UUID. A session begins only after the emulator process is spawned, increments play count transactionally, and finalizes elapsed monotonic time plus completed/failed/terminated outcome exactly once after process exit. The details pane exposes compact statistics and editable not-started/in-progress/completed/on-hold/abandoned state; Recently Played is a live sidebar/filter view ordered by the persisted latest session. State work stays off the Qt thread, updates optimistically with rollback, survives restart, and retains unfinished session rows rather than inventing time after an app interruption. |
| ROM folder scan, checksum matching, import progress, cancellation and library maintenance | `backend/src/scanner`, `src/components/rom_import.rs`, `import_progress.rs` | Implemented with a native folder picker, optional exact checksums, platform assignment, searchable/sortable preview, bulk and per-row selection, cooperative cancellation, persistent lossless paths, and immediate collection refresh. The recovered ZIP/7z/RAR behavior is stronger than the legacy first-extension peek: every safe recognized member is streamed without review-time extraction, shown separately from its container, and linked only on a unique SHA-1/MD5 identity. Sole-member archives remain directly launchable; multi-member archives select nothing automatically, and explicitly reviewed members are digest-revalidated and atomically materialized into a content-addressed cache with exact source provenance. Every readable row can also be explicitly linked through canonical/alternate-title catalog search with an exact platform filter and visible stable ID. Suggestions never establish identity; the reviewed link persists only after import and restores only when path, member, size, MD5, and SHA-1 remain exact. Empty, encrypted, multi-volume, duplicate, symlink, and unsafe members fail closed. 7z and RAR decoding are pure Rust and need no external utility. The native Library Audit reuses one prepared catalog index across every configured root and distinguishes exact healthy, content-changed, duplicate SHA-1, untracked, missing, unreadable, and disconnected-root states. Only fully scanned roots synchronize availability. Nothing merges, relinks, or deletes automatically; confirmed cleanup removes selected already-missing database rows and cannot touch ROMs. Named scan presets and resumable hash caches remain future work. |

## Minerva acquisition

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Detect Minerva catalog and resolve all bundles for a platform | `backend/src/handlers.rs` | Implemented with exact normalized mapped/provider names; Atari 800 and Arcade retain only their collection-bounded legacy rules. Cross-database numeric platform IDs are deliberately not joined. |
| Exclude already installed games by LaunchBox ID or stable game UUID | `backend/src/api.rs`, migration `0016_game_uid.sql` | Implemented in the native catalog layer |
| Fetch and cache torrent metadata, list files, rank title/region candidates | `backend/src/torrent/mod.rs`, `backend/src/handlers.rs` | Native details pane fetches bounded torrent metadata off-thread, validates and caches it for 24 hours in the OS cache directory, reuses one decoded index across active views, lists real files, and ranks review candidates by title strength, the persisted full region order, and latest/original revision policy. Exact variant labels and the best-match highlight are native. |
| Game-only and full-torrent modes | `backend/src/handlers.rs` | Implemented for reviewed exact-file selection and explicit whole-torrent mode |
| Related-file plans for multidisc, eXo, MAME laserdisc, Daphne and Hypseus | `backend/src/exo.rs`, `arcade.rs`, `handlers.rs` | Generic optical multi-disc plans are native: numeric/alphabetic/Roman markers, runnable-primary selection, CUE/CCD/MDS/GDI sidecars, exact-member confirmation, one durable aggregate queue item, preserved relative layout, idempotent preflight/import, and last-step M3U publication. eXoDOS/eXoWin3x/eXoWin9x acquisition is also native with exact game-data/metadata/utility dependencies, legacy DOS language priority, role-aware review, aggregate queueing, shared per-torrent staging, and exact-primary collection identity. Installed eXo archives can be prepared off-thread into a versioned atomic cache with title-specific metadata, DOS game data, nested utility extraction, shared MT-32/DOSBox/86Box/PCBox assets, strict ZIP path containment, cancellation cleanup, durable stable-ID lookup, native status/retry UI, and compatible emulator launch. MAME ROM/CHD and complete Hypseus/Daphne ROM/framefile/media plans are native, use canonical MAME set-name evidence, select only reviewed torrent members, and ingest into validated launch layouts. |
| qBittorrent Web API setup, connection testing and container path mapping | `backend/src/torrent/clients/qbittorrent.rs`, `state.rs` | Implemented with native settings, credential-store secrets, literal remote/container paths, bounded Web API requests, and an isolated `lunchbox` category |
| Queue progress, aggregate speed, pause, resume, cancel, delete and recent jobs | `src/components/minerva_download_queue.rs`, `backend/src/handlers.rs` | Persistent native queue, per-job and aggregate transfer speed, pause/resume, non-destructive cancel, confirmed individual record removal, and confirmed finished-history cleanup are implemented. Cleanup never removes active/pending-import jobs, pending post-import actions, torrents, or downloaded files. A native follow-client/pause-after-import seeding policy is durable, waits for every selected bundle member, retries after failure or restart, and uses qBittorrent's non-destructive stop action. The legacy app had no ratio/time policy to reproduce. |
| Completion ingestion and symlink, hardlink, reflink, copy or leave-in-place policy | `backend/src/handlers.rs`, `state.rs` | Implemented with collision checks, idempotent recovery, exact installed identity, and cross-platform path handling. Machine-media plans always materialize their launch layout; leave-in-place uses native symlinks so large qBittorrent-owned media is not copied. |
| BIOS/firmware inventory and acquisition | `backend/src/firmware.rs`, settings UI | Native runtime/core-scoped inventory preserves the legacy required/optional, HLE-fallback, target-strategy, source, exact-package, and manual-import model as 118 validated canonical rules across 17 sources. The game-details card downloads exact Minerva members, reviewed official HTTPS packages, or GitHub source archives and supports exact-name local import. Bounded traversal-safe extraction, an isolated package store, archive and per-file SHA-256 receipts, transactional state, qBittorrent progress recovery, native folder opening, and Linux/Windows/macOS runtime target adapters are implemented. The live Minerva metadata gate proves every inherited package path resolves to exactly one torrent member. |

## Game details and media

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Metadata, variants, notes, related actions and launch controls | `src/components/game_details.rs` | Native responsive details pane implements core metadata, Minerva source inspection, explicit download-variant labels/ranking, and real prepare/cancel/verify-refresh/detect/play workflows for installed eXo games. All alternate names linked to the record's positive LaunchBox database ID are shown with their source region and expand Minerva matching; candidate review discloses the winning lookup title. Alternate names never merge records, replace stable UUIDs, or auto-accept identity. Catalog-level variant navigation, editing, and other related actions remain in the ledger. |
| Box front, screenshots, title screens, fan art and clear logos | `backend/src/images`, `src/components/lazy_image.rs` | Existing standardized cache assets are indexed off-thread by stable LaunchBox ID and rendered asynchronously in grid, list, and details views. Missing visible assets use a bounded native LibRetro queue for box art, gameplay, title, and logo media. The unified native Find Artwork workflow supports reviewed SteamGridDB backgrounds/covers/logos and reviewed IGDB covers/artwork/screenshots. Results remain candidates until the user chooses the exact provider game; Lunchbox persists a separate stable provider ID, then the user chooses one safe static file. Minerva, EmuMovies, ScreenScraper, and WebSearch retrieval remain in the ledger. |
| Source priority, fallback, rotation, redownload and missing-media state | `backend/src/images/source_selector.rs`, `download_service.rs` | Deterministic local/LaunchBox/LibRetro/SteamGridDB/IGDB/Minerva/EmuMovies/ScreenScraper/WebSearch priority and media-type fallback are native. The complete provider order is validated, durable, and editable in the native Settings UI; saving starts an off-thread reindex that immediately governs every cached artwork, video, and manual candidate. The index preserves every valid candidate in that order, the details hero rotates candidates without rescanning, and explicit refresh bypasses the seven-day LibRetro miss cache while replacing only that provider's owned file. Reviewed SteamGridDB and IGDB selections share one bounded, signature-validating, atomic publisher that replaces only the selected provider and media kind. A miss or error keeps the prior image and every other provider. Cancellation of in-flight HTTP, whole-library missing-media audit, and the remaining retrieval providers stay in the ledger. |
| Manuals, videos, video progress and local-file opening | `backend/src/api.rs`, `video_player.rs` | Cached supplemental media is native. Rust discovers only canonicalized, non-empty files under the configured media root, applies deterministic provider/format priority, derives a bounded SHA-256 content identity from each video's length plus head/tail samples, and stores bounded resume progress by stable game UUID plus that identity. The Qt details card auto-plays muted/looped video with seek, sound, restart, fullscreen, pause-on-launch/close, and five-second durable checkpoints; manuals open through Qt's OS-native URL handler. The missing-manual card finds only high-confidence exact ZIP members in Minerva's dedicated manual-scan torrent, selects that member through the safe native qBittorrent client, persists byte progress, resumes after restart, cancels without deleting files or disturbing shared selections, validates the ZIP, and atomically publishes it into the owned media cache. Real current torrent metadata and an exact Cooking Pico match are covered by an unattended metadata probe; transfer persistence, shared selection, ZIP rejection, atomic publication, and the native missing-media surface have deterministic gates. Minerva coverage remains archive-dependent. EmuMovies video/manual fallback remains in the ledger pending authorized access to its current developer API. |
| 3D box viewer | `src/components/box_3d_viewer.rs` | Implemented as a native Qt Quick 3D details-hero mode using exact cached front/back covers, a deliberate front-cover fallback when no back exists, separate spine/edge geometry, automatic rotation, drag, wheel zoom, pause, reset, and an unattended real-cover UI probe. |

## Emulators and launching

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Host-aware emulator catalog and detection | `backend/src/emulator.rs`, `db/emulators.db` | Native for prepared PC installs and generic local ROMs: canonical platform aliases, exact core slugs, host-scoped package sources, native PATH, Lunchbox's isolated Nix profile, managed release receipts, Windows install directories, macOS app bundles, and Linux Flatpaks are resolved off the GUI thread. Linux Flatpak/RetroArch launch and native manager discovery are runtime-verified; Windows/macOS release-host verification remains. |
| Install, update, uninstall and launch | `backend/src/emulator.rs`, `src/components/emulator_updates.rs` | Native lifecycle UI and Rust services cover Linux Flatpak, isolated Nixpkgs profile, AppImage/AppImageUpdate, reviewed GitHub archive, and official Wine-backed downloads; Windows winget; macOS Homebrew; and exact host-specific Libretro core downloads. The recovered update manager behavior is native: update discovery preserves every exact installed backend and Flatpak scope, shows only proven current-to-available changes, selects rows explicitly, applies them sequentially, and retains per-row Pending/Updating/Updated/Failed state and errors. Package identities are passed as argument vectors without a shell, downloads are bounded and verified against GitHub size/digest metadata when published, archive paths are contained, and uninstall requires a Lunchbox ownership receipt. Prepared eXo, generic local-ROM, MAME set, Hypseus framefile, and managed native/Wine launch discovery share the same catalog. Discovery and UI are runtime-verified against a real isolated Nix RetroArch `1.21.0 -> 1.22.2` update without applying it; package mutation still requires Linux, Windows, and macOS release-host gates. |
| Platform and per-game emulator preferences | migrations `0008` and handler APIs | Native exact stable-emulator-ID/runtime/core preferences, with game precedence over normalized platform defaults and reset controls. |
| Runtime launch profiles and template overrides | migrations `0013` and `0014` | Native and durable by exact emulator ID/runtime/core at game, normalized-platform, and all-platform scopes. Each field inherits independently in game → platform → global order. Extra arguments augment built-in argv at the emulator-specific safe insertion point; a non-empty template replaces built-in argv and supports only validated context placeholders. Ordinary ROM, RetroArch, MAME, Hypseus, Altirra, eXoDOS/eXoWin3x DOSBox/ScummVM, and eXoWin9x DOSBox-X/86Box/PCBox plans preserve native/Flatpak/Wine path mapping and never invoke a shell. The contextual Qt editor shows built-in/effective behavior, inheritance, save/clear, and errors without blocking launch discovery. The native Launch Commands manager restores the legacy searchable global/platform matrix with a worker-loaded virtualized list, exact standalone/core rows, built-in/customized filters, inheritance preview, context validation, and focused save/clear; game profiles remain beside Play. Windows/macOS runtime-host verification remains a release gate. |
| RetroArch cores, standalone emulators, DOS/eXo and arcade layouts | `backend/src/emulator.rs`, `exo.rs`, `arcade.rs` | Generic local-ROM RetroArch/standalone, exact host-specific core acquisition, exact MAME set/ROM-path (including Arcade Pinball labels), validated Hypseus framefile, bounded direct-file FinalBurn/Flycast/Supermodel, eXoDOS/eXoWin3x DOSBox/ScummVM, and eXoWin9x DOSBox-X/86Box/PCBox-compatible layouts are native, shell-free, traversal-safe, and preserve exact native paths. Daphne-source acquisition normalizes to the Hypseus contract; a dedicated Daphne runtime, TeknoParrot, and other specialized machine profiles remain. |
| Controller discovery, per-player mapping and InputPlumber profiles | `src/components/controller_mapping.rs`, `backend/src/controllers.rs` | The native Linux slice now discovers physical joystick devices by exact sysfs identity, deliberately filters non-game and non-manageable virtual inputs, reports the real InputPlumber service and supported targets off-thread, and persists stable player order plus remap/pass-through/hide, profile, and per-player virtual-target choices in the Rust state database. The polished Qt settings surface has keyboard-focusable ordering and an actionable empty state. Its native visual custom-profile designer retains the legacy Xbox, PlayStation, and generic diagrams, exact target-to-source button assignment, identity fallback, two-button preset, edit/delete management, and direct assignment from a player row; deletion clears every exact reference instead of leaving a broken launch profile. Launch supervision resolves game/platform/default profile precedence, writes built-in or validated custom profiles idempotently, matches exact InputPlumber source paths, captures intercept/profile/target/order state before mutation, and restores it through an RAII session after process exit or every failure path. Real service discovery and the profile designer are verified on Linux; applying/restoring against attached managed hardware, native Windows/macOS host adapters, and full controller navigation remain release gates. |
| Firmware validation before launch | `backend/src/firmware.rs` | The selected emulator/core exposes required, optional, HLE, manual, imported, and synced state before launch. Required non-HLE packages disable Play until installed; Sync / Repair revalidates the archive plus every extracted file against stored SHA-256 and size receipts before atomically copying or merging into the exact runtime target. Manual-only configurations fail visibly and retain their reviewed instructions. Automated release-host verification is complete on Linux; native Windows/macOS target verification remains a release gate. |

## Settings, platform support and presentation

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| OS-native data/media/program/save directories | `backend/src/state.rs` | Native path handling established |
| Region priority and all provider/torrent settings | `src/components/settings.rs`, `backend/src/state.rs` | The complete legacy region order and latest/original revision choice are native, persisted, validated, migration-compatible, and consumed by live Minerva ranking. The polished Qt editor exposes every region with keyboard-focusable move controls, a default reset, explicit untagged-release placement, and alias/duplicate normalization. SteamGridDB and IGDB configuration, connection testing, secure removal, source attribution, and IGDB licensing guidance are native. Remaining provider settings stay in the ledger. |
| Credentials in the system keyring | `backend/src/keyring_store.rs` | Implemented for qBittorrent, SteamGridDB, and IGDB/Twitch through the operating-system credential store; no secret column exists in SQLite |
| Linux, macOS and Windows packaging | `.github/workflows/release.yml` | Nix Linux package works; native macOS/Windows runtime packaging remains in the ledger |
| Keyboard/controller-first full-screen presentation | Product requirement beyond the Electron desktop UI | Tracked in `FRONTEND_ROADMAP.md` |
| Loading minigame and GPU presentation | `src/components/minigame` | Behavior recorded |

## Port rule

A row moves to implemented only with real service behavior, native Qt UI,
persistent state where applicable, error and cancellation handling, tests, and a
measured release runtime. A disabled control, empty dialog, compile-only bridge,
or silent no-op never counts as parity.
