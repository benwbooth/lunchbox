# Legacy Lunchbox feature parity ledger

The `legacy/electron` branch is the behavioral source for Lunchbox-owned
features. The native port does not embed its web frontend or HTTP server, but it
must retain the useful behavior behind them. This ledger is checked whenever a
native vertical slice is completed so that features are not silently lost.

## Catalog and collection experience

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Full discoverable catalog separated from writable user state | `backend/src/state.rs`, `backend/src/api.rs` | Implemented as read-only catalog layers |
| Immediate library shell, platform artwork, and fast wheel navigation | `src/components/sidebar.rs`, `game_grid.rs` | Native. Startup publishes 240 real catalog rows, complete 191-platform counts, and local/Minerva coverage before building the full 303,560-row index in the background; the atomic replacement preserves active navigation and filters. All 216 legacy platform icons are compiled Qt resources, with explicit aliases for every current catalog spelling. Grid and list views share a transparent viewport-level wheel interceptor, repeated-notch acceleration, cumulative momentum, and bounded easing even when the pointer is over a delegate label. The 2× hover card expands real layout geometry, image request dimensions, and font metrics instead of scaling a rasterized card texture; it repositions at all four viewport edges and respects the sidebar and scrollbar, reducing its expansion only when the window cannot contain the full card. Title-derived color gradients are now true missing-art fallbacks: desktop and Couch Mode box views replace them with a neutral near-black mat as soon as any usable artwork URL exists, including during asynchronous decoding, so PreserveAspectFit margins never tint real cover art. |
| Platform sidebar, counts, search, grid/list views, zoom and artwork choice | `src/components/sidebar.rs`, `toolbar.rs`, `game_grid.rs` | Implemented with virtualized native views, live platform/result counts, alias-aware platform search, and a polished 180–400 px resizable sidebar. Search and width persist in cross-platform SQLite state through coalesced workers rather than browser storage or GUI-thread writes. Common legacy abbreviations remain available even when an older catalog has no alias table. Grid/list presentation, ordered visible columns, and deterministic Library or metadata ordering also persist; one Rust catalog worker performs the same ordering for both views, preserving explicit collection and recent-play semantics under Library order. The compact data-list has a sticky, keyboard-accessible dynamic header and a native manager for reordering, adding, removing, or resetting Title, Platform, Developer, Publisher, Year, Release Date, Genre, Players, Rating, ESRB, Co-op, Variants, Type, Series, Region, and Notes plus Lunchbox's Availability field. Its legacy multi-select exact-value filters are native for every column, compose with AND and the global filter stack, show worker-computed full-catalog counts in a bounded searchable dialog, and use compact adaptive All/None/include/exclude state instead of copying hundreds of thousands of values. Active headers and the global Filters popup make the session-only state visible and clearable. An aligned interned metadata store avoids adding 16 owned strings to every game, and decorated worker sorts normalize each effective value once rather than allocating in the comparator. Fresh, cold, and exact-facet 1920×1200 probes exercise Publisher/Title/Rating/Availability/Platform order, Publisher sorting, preference persistence, exact result counts, and real values against all 303,560 records. Card zoom persists from 50–200%, as does box front/back/3D, gameplay, title-screen, fan-art, or clear-logo choice. The excluded legacy MiniGames entry was deliberately not ported. |
| Installed, non-retail and adult filters | `src/components/toolbar.rs`, `backend/src/api.rs` | Implemented as composable native filters, including an explicit Minerva-covered/not-installed choice and persisted non-retail/adult preferences |
| Regional/version variants and alternate titles | `backend/src/api.rs`, `backend/src/db/schema.rs` | Native. Minerva candidates parse and display combined regions and revisions, normalize common Roman-numeral sequels, and apply the complete persisted region order plus latest/original revision preference without overriding a stronger title match. Exact-linked alternate names are browsable metadata and may expand candidate search without becoming identity. Catalog release families are bounded to the same exact platform and recognized trailing release tags, deduplicated by full display title, ordered by the same preferences, and switch through the target row's exact UUID. Grouping never merges rows or transfers provider IDs, metadata, install state, or acquisition state. |
| Favorites, playlists, normal collections and smart collections | `backend/migrations`, `src/components/sidebar.rs` | Favorites and both manual and smart collections are native and durable by stable game UUID. Manual collections preserve explicit member order and support per-game, reorder, and confirmed bulk visible-result changes. Smart shelves compose title, exact platform, installed/Minerva/catalog availability, favorite, completion, retail/non-retail/adult, and effective co-op supported/not-supported/unspecified rules and refresh when their source state changes. The polished two-pane manager, sidebar navigation, search/platform composition, asynchronous writes, optimistic rollback, restart recovery, and bounded portable JSON import/export are native. Portable matching accepts the exact Lunchbox UUID or an unambiguous positive LaunchBox database ID only; display titles never establish identity. Selecting a manual playlist or smart shelf adds a worker-computed overview with description/rules, exact full-shelf and visible counts, unique platforms, installed/Minerva-ready, favorites, played/completed, accumulated play time, and direct Edit/Export/Manage actions. Manual members have optional shelf-specific display titles and notes in a dedicated native editor; exact-ID child records survive reorder, cascade on removal, apply only in that manual shelf, and round-trip through backwards-compatible portable files without changing canonical metadata, matching, downloads, or launch behavior. Couch Mode browses every manual and smart shelf through a virtualized controller-first picker with live counts and description/rule context, routes and restores by exact collection ID, and safely falls back after deletion or a stale ID. |
| Play count, play time, last played, completion state and sessions | `backend/migrations`, `backend/src/handlers.rs` | Native and durable by stable game UUID. A session begins only after the emulator process is spawned, increments play count transactionally, and finalizes elapsed monotonic time plus completed/failed/terminated outcome exactly once after process exit. The details pane exposes compact statistics and editable not-started/in-progress/completed/on-hold/abandoned state; its polished bounded session inspector shows newest-first localized timestamps, exact emulator identity, compact duration, outcome badges, keyboard-accessible filters, and a virtualized list. It reloads from SQLite when a launch ends. Recently Played is a live sidebar/filter view ordered by the persisted latest session. State work stays off the Qt thread, updates optimistically with rollback, survives restart, and retains unfinished session rows rather than inventing time after an app interruption. |
| ROM folder scan, checksum matching, import progress, cancellation and library maintenance | `backend/src/scanner`, `src/components/rom_import.rs`, `import_progress.rs` | Implemented with a native folder picker, optional exact checksums, platform assignment, searchable/sortable preview, bulk and per-row selection, cooperative cancellation, persistent lossless paths, and immediate collection refresh. Reusable named scan profiles durably preserve the exact native root, platform hint, normalized extension scope, and checksum policy; modifying a loaded profile becomes an explicit one-time scan until it is saved again. **Scan all** prepares one catalog match index, visits saved profiles sequentially, remains cancellable, and durably retains up to 200 compact completed/cancelled/failed snapshots with exact profile scope, batch position, result/error counts, and cached/read work. Detailed rows are deliberately released between profiles; loading a historical profile performs the ordinary review scan through the validated hash cache. Deleting a profile preserves its historical snapshots. The recovered ZIP/7z/RAR behavior is stronger than the legacy first-extension peek: every safe recognized member is streamed without review-time extraction, shown separately from its container, and linked only on a unique SHA-1/MD5 identity. Sole-member archives remain directly launchable; multi-member archives select nothing automatically, and explicitly reviewed members are digest-revalidated and atomically materialized into a content-addressed cache with exact source provenance. Every readable row can also be explicitly linked through canonical/alternate-title catalog search with an exact platform filter and visible stable ID. Suggestions never establish identity; the reviewed link persists only after import and restores only when path, member, size, MD5, and SHA-1 remain exact. Empty, encrypted, multi-volume, duplicate, symlink, and unsafe members fail closed. 7z and RAR decoding are pure Rust and need no external utility. The native Library Audit reuses one prepared catalog match index across every configured root and distinguishes exact healthy, content-changed, duplicate SHA-1, untracked, missing, unreadable, and disconnected-root states. Only fully scanned roots synchronize availability. Nothing merges, relinks, or deletes automatically; confirmed cleanup removes selected already-missing database rows and cannot touch ROMs. |

The local-import parity slice also has a durable resumable checksum cache.
CRC32/MD5/SHA-1 results completed before cancellation persist in writable state;
regular files and complete archive-member inventories resume only when lossless
path, stable native filesystem identity, size, and OS change metadata agree.
Changed inputs are read again, malformed rows fail closed, warm hits do not
write, complete scans prune removed paths, and Qt reports cached versus read
containers. An extension-scoped scan marks only records inside that explicit
scope as missing; records for other recognized extensions remain present. ZIP
containers may be scoped independently from their members, and custom portable
extensions are supported without weakening archive safety or identity rules.

## Minerva acquisition

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Detect Minerva catalog and resolve all bundles for a platform | `backend/src/handlers.rs` | Implemented with exact normalized mapped/provider names; Atari 800 and Arcade retain only their collection-bounded legacy rules. Cross-database numeric platform IDs are deliberately not joined. |
| Exclude already installed games by LaunchBox ID or stable game UUID | `backend/src/api.rs`, migration `0016_game_uid.sql` | Implemented in the native catalog layer |
| Fetch and cache torrent metadata, list files, rank title/region candidates | `backend/src/torrent/mod.rs`, `backend/src/handlers.rs` | Native details pane fetches bounded torrent metadata off-thread, validates and caches it for 24 hours in the OS cache directory, reuses one decoded index across active views, lists real files, and ranks review candidates by title strength, the persisted full region order, and latest/original revision policy. Exact variant labels and the best-match highlight are native. |
| Game-only and full-torrent modes | `backend/src/handlers.rs` | Implemented for reviewed exact-file selection and explicit whole-torrent mode. Every GET action now opens the same native review surface, even for a single member. It displays exact selected/whole-torrent sizes, install or leave-in-place destination, storage mode, free-space result, and duplicate/collision failures; Queue remains disabled until that asynchronous native preflight succeeds. The complete preflight is repeated inside enqueue immediately before qBittorrent is changed. Couch Mode no longer exits to Desktop Mode for an available game: its controller-first download screen flattens the progressively published source groups without losing exact source/file indices, keeps the preservation-source order and best match first, performs the same asynchronous storage review, routes one-time qBittorrent setup explicitly, and queues through the identical Rust service. Back returns to the candidate list or shelf without changing the selected game. |
| Lawful user-supplied torrent intake | Product expansion beyond the legacy Minerva-only flow | Native and provider-neutral. The main navigation accepts one local `.torrent` or v1 magnet, locks an exact platform, exposes source name, torrent name, v1 info hash, and the complete bounded file inventory, then registers only its metadata and normalized member index without queueing payload. Exact canonical or exact-linked alternate title keys expose matching members in the ordinary per-game Get review; the selected member alone is queued even if whole-torrent mode is enabled. External labels never create catalog identities. Rust parsing remains bounded/off-thread and rejects unsafe, duplicate, case-ambiguous, or Linux/macOS/Windows-incompatible paths. The owned qBittorrent category, durable queue, seeding policy, and SQLite source/torrent/member/job provenance are shared. Unit tests cover parser rejection, magnet identity, idempotent no-download registration, exact platform/title resolution, selective enforcement, exact-game provenance, and real Web API enqueue sequences. Watched folders, provider manifests, and authorized adapters remain roadmap items. |
| Related-file plans for multidisc, eXo, MAME laserdisc, Daphne and Hypseus | `backend/src/exo.rs`, `arcade.rs`, `handlers.rs` | Generic optical multi-disc plans are native: numeric/alphabetic/Roman markers, runnable-primary selection, CUE/CCD/MDS/GDI sidecars, exact-member confirmation, one durable aggregate queue item, preserved relative layout, idempotent preflight/import, and last-step M3U publication. eXoDOS/eXoWin3x/eXoWin9x acquisition is also native with exact game-data/metadata/utility dependencies, legacy DOS language priority, role-aware review, aggregate queueing, shared per-torrent staging, and exact-primary collection identity. Installed eXo archives can be prepared off-thread into a versioned atomic cache with title-specific metadata, DOS game data, nested utility extraction, shared MT-32/DOSBox/86Box/PCBox assets, strict ZIP path containment, cancellation cleanup, durable stable-ID lookup, native status/retry UI, and compatible emulator launch. MAME ROM/CHD and complete Hypseus/Daphne ROM/framefile/media plans are native, use canonical MAME set-name evidence, select only reviewed torrent members, and ingest into validated launch layouts. |
| qBittorrent Web API setup, connection testing and container path mapping | `backend/src/torrent/clients/qbittorrent.rs`, `state.rs` | Implemented with native settings, credential-store secrets, literal remote/container paths, bounded Web API requests, and an isolated `lunchbox` category. Enqueue preflight reads the current category and exact file priorities to reject an already-owned job or equivalent selected member set, tolerating only qBittorrent's optional single torrent-root prefix rather than fuzzy path matching. |
| Queue progress, aggregate speed, pause, resume, cancel, delete and recent jobs | `src/components/minerva_download_queue.rs`, `backend/src/handlers.rs` | Persistent native queue, per-job and aggregate transfer speed, pause/resume, non-destructive cancel, confirmed individual record removal, and confirmed finished-history cleanup are implemented. A compact lower-right status opens automatically for new work, remains hideable, and shows active progress plus recent completed jobs; its full manager retains history, recovery, cleanup, and all controls. Failed rows expose an exact stable-ID Retry action and a newest-first durable transition/outcome history. Before qBittorrent is mutated, every queue operation durably retains the exact bounded `.torrent` bytes with SHA-256 and v1-info-hash receipts. Recovery rebuilds the complete reviewed selection across related jobs, preserves the exact client save path and plans, and rechecks/resumes an existing Lunchbox-owned transfer. If an active owned torrent was removed, refresh recreates it automatically from those retained bytes; explicit Retry does the same for failed jobs. A foreign torrent with the same hash is never adopted or modified, and an older unretained manual record fails actionably instead of searching by title or guessing replacement metadata. Cleanup never removes active/pending-import jobs, pending post-import actions, torrents, or downloaded files; removing a terminal record cascades only its event history. A native follow-client/pause-after-import seeding policy is durable, waits for every selected bundle member, retries after failure or restart, and uses qBittorrent's non-destructive stop action. The legacy app had no ratio/time policy to reproduce. Opt-in retry schedules for other transient failures remain roadmap work. |
| Completion ingestion, file management, and symlink, hardlink, reflink, copy or leave-in-place policy | `backend/src/handlers.rs`, `state.rs`, game details | Implemented with collision checks, idempotent recovery, exact installed identity, and lossless cross-platform path handling. Native preflight checks the actual filesystem that will receive download and installation bytes, uses host-native free-space APIs, applies a bounded reserve, and avoids charging ordinary leave-in-place jobs for an unused ROM destination; arcade machine layouts still reserve their required staged destination. Every new installed plan member and generated playlist receives a type, size, SHA-256, creator, and reference-aware ownership receipt. The native Game Files card opens the selected file's directory through Qt, removes a legacy or user-owned association without deleting content, and uninstalls only unchanged Lunchbox-created files at their last game reference. A changed owned file refuses the whole deletion set; shared, imported, pre-existing, and leave-in-place files remain. Machine-media plans always materialize their launch layout; leave-in-place uses native symlinks so large qBittorrent-owned media is not copied. Prepared eXo removal is constrained to the exact per-game signature directory and preserves shared bootstrap state. A fresh/cold-start real-catalog probe proves the receipt, confirmation, exact file removal, library refresh, and durable restored state. |
| BIOS/firmware inventory and acquisition | `backend/src/firmware.rs`, settings UI | Native runtime/core-scoped inventory preserves the legacy required/optional, HLE-fallback, target-strategy, source, exact-package, and manual-import model as 124 validated canonical rules across 19 sources. The game-details card downloads exact Minerva members, reviewed official HTTPS packages, or GitHub source archives and supports exact-name local import. Bounded traversal-safe extraction, an isolated package store, archive and per-file SHA-256 receipts, transactional state, qBittorrent progress recovery, native folder opening, and Linux/Windows/macOS runtime target adapters are implemented. Nintendo Switch setup accepts only user-supplied `prod.keys` and firmware ZIPs, validates their production-key/NCA structure, and syncs them to the selected Eden, Ryubing, or Torzu profile without locating or downloading copyrighted material. A cancellable whole-library readiness audit verifies every distinct local platform/emulator/core choice, including exact per-game overrides, without collapsing required, manual, optional, ready, missing-runtime, or error states; its virtualized Qt surface routes reviewed rows back to the existing exact game workflow. The live Minerva metadata gate proves every inherited package path resolves to exactly one torrent member. |

## Game details and media

The current Qt details pane automatically inspects every Minerva torrent source
off the GUI thread and renders each source with its ranked download rows directly
underneath; the strongest source and candidate are first, so no preliminary
source-selection click is required. Its exact-release strip supports drag,
vertical or horizontal wheel/touchpad momentum, an always-visible interactive
scrollbar, and Left/Right/Home/End navigation. Search fields across the primary
library and management surfaces provide visible and Escape-key clear actions.

Grid preview hover ownership follows the repositioned expanded card rather than
its original grid cell, preventing the player from being disarmed when the card
moves under the pointer. A newly hovered or opened game's EmuMovies transfer now
starts immediately after canceling and safely requeueing the prior transfer;
per-worker generations reject stale progress and completion events.

Couch Mode also reuses already cached EmuMovies soundtracks after a 650-ms
stable-selection gate. Fast browsing remains silent, launch and picker surfaces
stop playback, and the now-playing, P-key, Game Menu, persisted enable, and
volume controls never initiate an implicit media download.

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Metadata, variants, notes, related actions and launch controls | `src/components/game_details.rs` | Native responsive details pane implements core metadata, exact catalog release navigation, Minerva source inspection, explicit download-variant labels/ranking, and real prepare/cancel/verify-refresh/detect/play workflows for installed eXo games. The polished three-tab native editor durably overlays display title, sort title, description, release date, developer, publisher, genre, players, rating, age rating, release type, series, region, play mode, version, release status, exact tri-state cooperative-play status, notes, bounded per-game tags, and ordered custom fields by stable UUID; search, sorting, exact list filters, smart title/tag/co-op rules, desktop details, and Couch Mode update immediately. Desktop details browse a series through one exact-value action; release fields never establish or merge identity, and internal catalog lifecycle values are not presented as release status. The real discovery catalog contributes 174,463 definite co-op values; unspecified rows remain unknown rather than being guessed. Tags deduplicate case-insensitively, appear as clickable details chips, and compose as an exact library filter. Custom-field names are case-insensitively unique per game, names and values participate in text search, order is explicit and keyboard/pointer editable, and the details pane presents the saved order. Metadata, tags, and custom fields validate before one SQLite transaction, so a rejected field cannot partially publish the rest of the profile. Canonical title/platform/provider/file identity stays immutable and continues to govern matching, downloads, artwork, activity, and launching; restoring catalog values deletes only the metadata overlay and retains tags and custom fields. The release carousel preserves separate exact UUIDs and visibly distinguishes current, installed, Minerva, and catalog-only rows. All alternate names linked to the record's positive LaunchBox database ID are shown with their source region and expand Minerva matching; candidate review discloses the winning lookup title. Neither release grouping nor alternate names merge records, replace stable UUIDs, transfer metadata, or auto-accept identity. The native **More Like This** shelf ranks at most 12 exact catalog records by series, normalized title/franchise terms, developer, publisher, genre, platform, play mode, and release proximity; applies the effective local metadata overlay and adult/non-retail preferences; excludes same-platform regional/version siblings already represented by Releases; and discloses the strongest relationship plus Installed/Minerva/Catalog availability. Its bounded query and availability join run on the existing details worker, while pointer, keyboard, and accessibility routes all open the chosen stable UUID without fuzzy identity transfer. |
| Catalog ratings, provenance and related web actions | `src/components/game_details.rs` | Native. The legacy community-vote count and Video/Wikipedia actions are restored, alongside exact Steam-app actions and visible source provenance. Real-catalog coverage is 174,463 vote counts, 75,647 videos, 45,150 Wikipedia links, 22,853 Steam IDs, and 303,560 source labels. Rust accepts only bounded, credential-free public HTTP(S) URLs and never invents a link. Desktop opens them through Qt's system browser; Couch Mode retains vote/source context and the explicit Desktop Details handoff. Fresh and cold-start real-catalog probes assert the exact Super Mario Bros. facts and capture the fully accessible source card. |
| Box front, screenshots, title screens, fan art and clear logos | `backend/src/images`, `src/components/lazy_image.rs` | Existing standardized cache assets are indexed off-thread by stable LaunchBox ID and rendered asynchronously in grid, list, and details views. Missing exact visible assets use a bounded native queue that follows the configured provider order through LibRetro, SteamGridDB, IGDB, exact EmuMovies archive members, and ScreenScraper. Reviewed IDs are reused directly; otherwise automatic retrieval requires one unambiguous strict exact SteamGridDB title, one strict exact IGDB title/platform pair, or one strict exact ScreenScraper title inside a mapped platform, and never persists that transient result as an identity link. Nintendo Switch skips LibRetro's nonexistent Switch namespace and continues through every configured modern-game provider. Ambiguous provider search results remain candidates until the user chooses the exact provider game and Lunchbox persists its separate stable provider ID. The legacy first-result DuckDuckGo scraper is replaced by an explicit Web source: Lunchbox opens a focused Google Images query, accepts one reviewed HTTPS URL or local file, downloads or copies it off-thread into a per-candidate quarantine, enforces a 16 MiB limit plus PNG/JPEG/WebP signatures before preview, then atomically promotes those exact reviewed bytes into only the `websearch` cache. It never infers game identity, scrapes results, or silently selects a thumbnail. The legacy EmuMovies FTP client, platform/title matcher, artwork packs, and arcade parent/clone lookup are native; member credentials remain in the OS keyring and FTP work stays off the Qt thread. |
| Source priority, fallback, rotation, redownload and missing-media state | `backend/src/images/source_selector.rs`, `download_service.rs` | Deterministic local/LaunchBox/LibRetro/SteamGridDB/IGDB/Minerva/EmuMovies/ScreenScraper/WebSearch priority and media-type fallback are native. The complete provider order is validated, durable, and editable in the native Settings UI; saving starts an off-thread reindex that immediately governs every cached artwork, video, and manual candidate. The index preserves every valid candidate in that order, the details hero rotates candidates without rescanning, and explicit refresh bypasses fresh provider-miss markers while replacing only the successful provider's owned file. Reviewed SteamGridDB, IGDB, and explicit Web selections share bounded, signature-validating, atomic publication that replaces only the selected provider and media kind. A miss or error keeps the prior image and every other provider. The bounded two-worker artwork scheduler has a separate foreground lane: an opened, selected, or sustained-hover game always runs before later background visibility requests, and requesting an already queued exact game/category promotes that work rather than silently ignoring the renewed intent. The native Missing Media Audit covers My Collection or the entire catalog one exact category at a time, remains cancellable and virtualized at 303k records, separates rows with exact LibRetro identity from explicit provider-review rows, and runs confirmed selections through the same bounded exact provider chain. The exact selected records and results are transactionally journaled around provider work, one stable lease serializes repair across Lunchbox processes, abandoned in-flight records safely return to pending, completed files remain terminal, and restart presents a compact Resume/Discard decision without starting network work automatically. The real Qt recovery probe verifies the restored game identity, count, dialog, and controls. Cancellation of active HTTP and bulk multi-provider review stay in the ledger. |
| Manuals, videos, video progress and local-file opening | `backend/src/api.rs`, `video_player.rs`, `src/components/game_grid.rs` | Cached supplemental media is native. Rust indexes only canonicalized, non-empty files, applies deterministic provider/format priority, derives bounded content identity, and stores resume progress by stable game UUID plus video identity. The Qt details card provides muted/looped playback, seek, sound, restart, fullscreen, pause-on-launch/close, and durable checkpoints; manuals open through Qt's OS-native URL handler. Grid hover uses a strict 500-ms intent gate, generation-guarded off-thread exact-ID lookup, one shared player, muted-by-default audio, visible provenance, and cancellation on leave, open, or delegate reuse. Recycled visible grid/list rows request artwork only and never enqueue gameplay video. Opening a game requests its exact video immediately; pointer hover must survive the full delay before that title enters the bounded EmuMovies FTP queue. Detail and hover playback restore the legacy bounded local-load recovery behavior through one tested QML controller: a failed exact URL is cleared and reloaded at most twice, successful load/play resets the budget, game changes cancel pending work, and only exhaustion becomes a visible error. A newer selected or sustained-hover title cooperatively interrupts the less relevant active FTP lookup/transfer, removes its unpublished partial file, places the interrupted title safely behind the foreground work, and moves it back to the front if the user returns before cancellation completes. Cancellation is checked while listing remote folders, building an uncached index, and streaming bytes, so priority does not wait for the whole previous lookup. Cards expose setup/queue/download/miss state, successful publication re-runs cache lookup for playback, and transient FTP failures remain retryable. The unattended hover probe proves no preview starts halfway through the intent delay before requiring the real video track, duration, and `PlayingState`. Missing manuals retain exact Minerva member selection, resumable qBittorrent progress, safe cancellation, ZIP validation, and atomic provider-owned publication. Direct EmuMovies artwork, video, music, and manual work remains cancellable with determinate progress, guarded partial cleanup, atomic publication, and streamed artwork-pack extraction. |
| EmuMovies game music | Product enhancement over the legacy desktop | Native. Rust searches only EmuMovies' individually downloadable HyperAudio tree, reuses the guarded platform/title matcher, rejects multipart archives and unsupported extensions, and maps each remote track to a deterministic SHA-256-derived provider-owned cache filename plus a bounded title sidecar. Downloads are cancellable, locked per track, cleaned on failure, and atomically published. The dedicated Qt Game Music card exposes setup routing, discovery, progress, previous/play/pause/next, seek, title/source/count, and provider-aware cached playback. A saved-account probe requires a real exact Super Mario Bros. discovery/download and ordinary supplemental-media indexing; a Qt probe requires playable audio through the native card. |
| 3D box viewer | `src/components/box_3d_viewer.rs` | Implemented as a native Qt Quick 3D details-hero mode using exact cached front/back covers, a deliberate front-cover fallback when no back exists, separate spine/edge geometry, automatic rotation, drag, wheel zoom, pause, reset, and an unattended real-cover UI probe. |

## Emulators and launching

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| Host-aware emulator catalog and detection | `backend/src/emulator.rs`, `db/emulators.db` | Native for prepared PC installs and generic local ROMs: canonical platform aliases, exact core slugs, host-scoped package sources, native PATH, Lunchbox's isolated Nix profile, managed release receipts, Windows install directories, macOS app bundles, and Linux Flatpaks are resolved off the GUI thread. Linux Flatpak/RetroArch launch and native manager discovery are runtime-verified; Windows/macOS release-host verification remains. |
| Install, update, uninstall and launch | `backend/src/emulator.rs`, `src/components/emulator_updates.rs` | Native lifecycle UI and Rust services cover Linux Flatpak, isolated Nixpkgs profile, AppImage/AppImageUpdate, reviewed GitHub archive, and official Wine-backed downloads; Windows winget; macOS Homebrew; and exact host-specific Libretro core downloads. The recovered update manager behavior is native: update discovery preserves every exact installed backend and Flatpak scope, shows only proven current-to-available changes, selects rows explicitly, applies them sequentially, and retains per-row Pending/Updating/Updated/Failed state and errors. Every install/update/uninstall is serialized across Lunchbox instances by one stable lease and recorded in an exact durable operation journal before mutation; terminal results are written once, the newest 200 remain inspectable, abandoned running rows reconcile as interrupted only when no live owner holds the lease, and the exact installation is rescanned before another action. The manager presents compact recent activity, while update batches offer **Stop after current** without killing an external package-manager transaction or discarding later selections. Exact host/manager/package/install-path pins are durable and idempotent; pinned updates remain visible, non-actionable, excluded from startup prompts, and directly reversible. They constrain Lunchbox batches without claiming to lock an external operating-system package manager. Package identities are passed as argument vectors without a shell, downloads are bounded and verified against GitHub size/digest metadata when published, archive paths are contained, and uninstall requires a Lunchbox ownership receipt. Prepared eXo, generic local-ROM, MAME set, Hypseus framefile, and managed native/Wine launch discovery share the same catalog. Discovery, pin/unpin, and UI are runtime-verified against a real isolated Nix RetroArch `1.21.0 -> 1.22.2` update without applying it; recovery presentation is verified by an isolated real Qt probe without applying an update. Package mutation still requires Linux, Windows, and macOS release-host gates. |
| Platform and per-game emulator preferences | migrations `0008` and handler APIs | Native exact stable-emulator-ID/runtime/core preferences, with game precedence over normalized platform defaults and reset controls. |
| Runtime launch profiles and template overrides | migrations `0013` and `0014` | Native and durable by exact emulator ID/runtime/core at game, normalized-platform, and all-platform scopes. Each field inherits independently in game → platform → global order. Extra arguments augment built-in argv at the emulator-specific safe insertion point; a non-empty template replaces built-in argv and supports only validated context placeholders. Ordinary ROM, RetroArch, MAME, Hypseus, Altirra, eXoDOS/eXoWin3x DOSBox/ScummVM, and eXoWin9x DOSBox-X/86Box/PCBox plans preserve native/Flatpak/Wine path mapping and never invoke a shell. The contextual Qt editor shows built-in/effective behavior, inheritance, save/clear, and errors without blocking launch discovery. A shared live preview exposes the selected runtime plus exact numbered argv template tokens, reports invalid quoting or unavailable placeholders inline, and disables saving invalid drafts; executable paths and host wrappers still resolve at Play. The native Launch Commands manager restores the legacy searchable global/platform matrix with a worker-loaded virtualized list, exact standalone/core rows, built-in/customized filters, inheritance preview, context validation, and focused save/clear; game profiles remain beside Play. Windows/macOS runtime-host verification remains a release gate. |
| RetroArch cores, standalone emulators, DOS/eXo and arcade layouts | `backend/src/emulator.rs`, `exo.rs`, `arcade.rs` | Generic local-ROM RetroArch/standalone, exact host-specific core acquisition, exact MAME set/ROM-path (including Arcade Pinball labels), validated Hypseus framefile, bounded direct-file FinalBurn/Flycast/Supermodel, eXoDOS/eXoWin3x DOSBox/ScummVM, and eXoWin9x DOSBox-X/86Box/PCBox-compatible layouts are native, shell-free, traversal-safe, and preserve exact native paths. Daphne-source acquisition normalizes to the Hypseus contract; a dedicated Daphne runtime, TeknoParrot, and other specialized machine profiles remain. |
| Complete RetroArch shader-pack installation | `src/components/settings.rs`, commit `98f0ef1` on `legacy/electron` | Native Settings installs the official Libretro Slang and GLSL collections into separate canonical directories. Rust performs bounded HTTPS/cache verification, safe ZIP validation, cancellable staging, receipts, explicit confirmation before replacing unmanaged official packs, multi-target rollback, and shell-free native folder opening. Custom sibling folders remain untouched. Unit tests cover idempotence, cancellation, unsafe archives, unmanaged-pack confirmation, and cross-target rollback; an isolated real-source Qt probe installed 6,564 verified files (101.1 MiB) and a second probe reused byte-identical cached archives. Windows/macOS target discovery compiles but remains a release-host runtime gate. |
| Controller discovery, per-player mapping and InputPlumber profiles | `src/components/controller_mapping.rs`, `backend/src/controllers.rs` | The native Linux slice now discovers physical joystick devices by exact sysfs identity, deliberately filters non-game and non-manageable virtual inputs, reports the real InputPlumber service and supported targets off-thread, and persists stable player order plus remap/pass-through/hide, profile, and per-player virtual-target choices in the Rust state database. The polished Qt settings surface has keyboard-focusable ordering and an actionable empty state. Its native visual custom-profile designer retains the legacy Xbox, PlayStation, and generic diagrams, exact target-to-source button assignment, identity fallback, two-button preset, edit/delete management, and direct assignment from a player row; deletion clears every exact reference instead of leaving a broken launch profile. Launch supervision resolves game/platform/default profile precedence, writes built-in or validated custom profiles idempotently, matches exact InputPlumber source paths, captures intercept/profile/target/order state before mutation, and restores it through an RAII session after process exit or every failure path. Real service discovery, the profile designer, and Couch Mode's separate direct cross-platform input backend are verified on Linux without attached hardware; applying/restoring against attached managed hardware and native Windows/macOS host adapters remain release gates. |
| Firmware validation before launch | `backend/src/firmware.rs` | The selected emulator/core exposes required, optional, HLE, manual, imported, and synced state before launch. Required non-HLE packages disable Play until installed; Sync / Repair revalidates the archive plus every extracted file against stored SHA-256 and size receipts before atomically copying or merging into the exact runtime target. Manual-only configurations fail visibly and retain their reviewed instructions. Automated release-host verification is complete on Linux; native Windows/macOS target verification remains a release gate. |

## Settings, platform support and presentation

| Legacy behavior | Source | Native state |
| --- | --- | --- |
| OS-native data/media/program/save directories | `backend/src/state.rs` | Native path handling established |
| Region priority, provider/torrent settings, and portable profile management | `src/components/settings.rs`, `backend/src/state.rs` | The complete legacy region order and latest/original revision choice are native, persisted, validated, migration-compatible, and consumed by live Minerva ranking. Settings is a polished full-window Qt section with a persistent navigation rail, direct section routing, bounded responsive rows, an explicit qBittorrent connection test, and Rust-backed operating-system directory pickers. The connection test reads qBittorrent's default save path, automatically uses it for a same-host client, and applies the portable managed suffix `lunchbox/roms`; container and remote clients expose only the required host-base mapping. The editor exposes every region with keyboard-focusable move controls, a default reset, explicit untagged-release placement, and alias/duplicate normalization. SteamGridDB, IGDB, and authorization-gated ScreenScraper configuration, connection testing, secure removal, and source guidance are native. IGDB setup identifies the required Confidential Twitch application Client ID and newest Client Secret rather than accepting an account login or MCP credential. The legacy Show/Hide behavior is restored consistently across qBittorrent, SteamGridDB, IGDB, EmuMovies, and both ScreenScraper secret fields; it reveals only the value currently being entered and never repopulates a keyring secret into QML. Shared native progress and multiline-editor controls avoid platform-style layout failures while retaining keyboard selection, wrapping, scrolling, and accessibility. A native profile card creates a consistent, versioned, receipt-checked backup of all writable collection state and owned Couch Mode themes, validates it before publication or staging, and applies a reviewed restore before models open on the next launch with rollback on failure. Credential-store secrets and large content stores remain local; lossless host-native paths are audited rather than rewritten across operating systems. Remaining provider settings stay in the ledger. |
| Credentials in the system keyring | `backend/src/keyring_store.rs` | Implemented for qBittorrent, SteamGridDB, IGDB/Twitch, EmuMovies, and ScreenScraper through the operating-system credential store; no secret column exists in SQLite |
| First-run setup and actionable missing-account states | Product onboarding requirement | Native. New profiles open a dedicated setup page for ROM storage, qBittorrent, SteamGridDB, IGDB, and EmuMovies; optional providers are identified as optional, account links use the OS browser, and every later missing-credential state routes directly to its Settings section. Completion is durable, while existing profiles migrate without an unexpected onboarding interruption. |
| Linux, macOS and Windows packaging | `.github/workflows/native-packages.yml` | Native CI builds and verifies the Nix package, Flatpak bundle, AppImage, Windows MSI and portable ZIP, plus signed-structure-checked x86_64 and arm64 macOS DMGs. The mounted DMG payload, executable architecture, embedded catalog, Applications link, and code-signature structure are validated on their native runners. Tagged release publication remains deliberately gated on a tag. |
| Keyboard/controller-first full-screen presentation | Product requirement beyond the Electron desktop UI | **Couch Mode** is the native living-room interface over the same CXX-Qt catalog, details, media, favorites, filters, downloads, and launch services as desktop mode. It provides persistent virtualized cinematic-wheel and horizontal-cover-shelf presentations with stable-UUID selection preservation, fanart/metadata, Play and Minerva handoff, true fullscreen, and mouse-free keyboard/direct-gamepad operation. Exact platform and manual/smart-collection pickers expose live context and restore by stable identity. Its couch-native Details and seven-action Game Menu include the real primary action, favorites, Desktop Details, exact release picker, Attract Mode, layout switching, and safe browsing return. Launch status, validated Attract settings, built-in/declarative themes, and direct hotplug-aware gamepad routing remain shared and restart-safe. Deterministic real-QML probes verify vertical wheel movement, both layout transitions without identity loss, cold layout restoration, release routing, platform/collection/Attract/theme persistence, and visual 1920x1200 captures against the real catalog. Attached-device Linux/Windows/macOS certification, pause/startup/shutdown screens, sound, multi-monitor marquees, parental controls, and richer couch-safe settings remain in `FRONTEND_ROADMAP.md`. |
| Loading minigame and GPU presentation | `src/components/minigame` | Intentionally excluded from the native product. Empty and loading states remain focused on browsing, collection status, and actionable progress rather than running a background game simulation. |

## Port rule

A row moves to implemented only with real service behavior, native Qt UI,
persistent state where applicable, error and cancellation handling, tests, and a
measured release runtime. A disabled control, empty dialog, compile-only bridge,
or silent no-op never counts as parity.
