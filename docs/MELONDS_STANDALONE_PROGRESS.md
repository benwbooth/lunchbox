# Standalone melonDS controller mapping

## Shared native launch dispatch

Connected saved melonDS setups to shared launch preparation, retained native
session ownership, spawning and health checks. UI/coverage now identify partial
Linux standard-button support. Twelve DS buttons are mapped; stylus, microphone,
additional instances, other hosts and runtime behavior remain incomplete or
unverified. The TOML dependency lock resolution remains pending. No tests/builds,
probes or launches ran. Formatting and diff whitespace checks only.
Coverage: 93/94 RetroArch profiles (98.9%) and 14/249 partial standalone
integrations (5.6%). These counts do not establish tested or cross-platform support.

## Native spawn and startup ownership checks

Added retained spawning with cancellation/failure cleanup and startup checks for
the selected native executable, loaded SDL library, generated configuration mount
identity and opened physical controller descriptors. Source configuration and
device routing are rechecked after handoff. These are Linux implementation
checks, not evidence of gameplay correctness or Windows/macOS support. Shared
launch dispatch remains pending. No tests/builds/probes/launches ran.
Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249 partial standalone
integrations (5.2%).

## Native command preparation

Connected SDL2 capture, configuration-path resolution and private TOML preparation
to a retained Bubblewrap command. The command overlays only the native config,
preserves cwd/runtime data, verifies trusted executable and ROM identity, and
retains source/runtime guards. Setups now require an absolute bubblewrap_program.
Spawning/startup confirmation and shared dispatch remain pending. No tests/builds,
probes or launches ran. Coverage remains 93/94 RetroArch profiles (98.9%) and
13/249 partial standalone integrations (5.2%).

## Retained SDL2 capture

Added launch-only SDL2 inventory and selected physical binding capture, resolving
saved stable identities through physical paths. The session retains native device
indices, measured bindings, topology and probe/runtime identity checks, and
recaptures enumeration before handoff. Explicit dependency preloading is rejected
because the shared SDL2 probe does not implement it; this field is not silently
ignored. Native command preparation and shared dispatch remain pending.
No tests/builds/probes/launches ran. Coverage remains 93/94 RetroArch profiles
(98.9%) and 13/249 partial standalone integrations (5.2%).

## Native Linux configuration path

Added retained portable-directory and ordinary XDG/HOME configuration lookup,
checking the selected native melonDS.toml against the saved source before use.
Relative XDG roots and missing/relative HOME fallback are explicitly rejected;
Qt-specific fallback behavior is not guessed. Selection and inherited environment
are rechecked before handoff. Capture and shared native launch dispatch remain
pending. No tests/builds/probes/launches ran. Coverage remains 93/94 RetroArch
profiles (98.9%) and 13/249 partial standalone integrations (5.2%).

## File-only configuration overlay

Pinned main.cpp pathInit and Platform.cpp show configuration shares the native
data directory with runtime files. Added mount-argument generation that overlays
only the verified native melonDS.toml with the private writable copy, preserving
the surrounding data directory. Saved source/native target identity must match.
Post-startup source checks are separate from generated-content checks because
native writes to the private copy are legitimate. Runtime path resolution and
shared native session dispatch remain pending. No tests/builds/probes/launches.
Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249 partial standalone
integrations (5.2%).

## Retained private configuration preparation

Connected saved calibrated sources and measured SDL2 device indices to the TOML
writer. Preparation snapshots a bounded source file, creates an owned temporary
melonDS.toml, and rechecks source identity/content and generated bytes before
handoff. Existing user configuration is not modified. Runtime configuration
selection, capture and shared launch dispatch remain pending. No tests/builds,
probes or launches ran; no temporary runtime configuration was instantiated.
Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249 partial standalone
integrations (5.2%).

## Shared visual setup editor

Connected melonDS setup JSON loading, review and staging to the shared editor,
including source/destination diagrams. Help specifies the twelve standard DS
controls and explicitly separates unimplemented stylus/lid/microphone mapping.
The editor labels native launch integration pending. No tests/builds/probes or
UI launches ran. Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249
partial standalone integrations (5.2%); melonDS is not counted yet.

## Saved setup persistence and review

Added saved primary-instance controller setups, settings validation and bounded
JSON review/staging methods. Review links all twelve destinations to calibrated
source controls and the existing DS visual layout, explicitly excluding stylus
and other non-button coverage. The editor entry and native launch dispatch remain
pending. No tests/builds/probes/launches ran. Coverage remains 93/94 RetroArch
profiles (98.9%) and 13/249 partial standalone integrations (5.2%).

## Physical binding translation

Connected calibrated source controls to measured SDL2 classic/evdev numbering,
released-state checks and native button/hat/axis encoding. All twelve destination
links are required. Axis activation must cross the exact native threshold; a
negative-rest positive-going trigger uses the native trigger mode. Invalid native
indices and held controls fail explicitly. No tests/builds/probes/launches ran.
Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249 partial standalone
integrations (5.2%); melonDS persistence and launch integration remain pending.

## Native TOML configuration

Added a parsed TOML editor for Instance0.Joystick and JoystickID. It requires all
twelve native button keys with distinct encodings, clears all pinned joystick
hotkeys to prevent overlapping actions, and preserves keyboard and unrelated
settings. The function only returns a private-copy configuration string; it does
not alter source files. Added toml_edit as a direct dependency; dependency lock
resolution remains pending. No tests/builds/probes/launches ran.
Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249 partial standalone
integrations (5.2%); melonDS launch integration remains pending.

Source pin: melonDS-emu/melonDS 906e9ebb27da8c6a715cd7abab4abfe8a8d29427.
Inspected frontend/qt_sdl/EmuInstanceInput.cpp and Config.cpp.

Added all twelve standard DS button routes and native raw SDL2 button/hat/axis
encoding, including the low-word unbound sentinel and exact positive, negative
and trigger thresholds. Button encodings that alias hats are rejected. Native
JoystickID is a device enumeration index, not a persistent identity; capture and
startup routing must account for native fallback to index zero on disappearance.
Config writing, persistence, UI and launch dispatch remain pending. This step
does not implement DS touch or microphone input. No tests/builds/probes/launches.

Coverage remains 93/94 RetroArch profiles (98.9%) and 13/249 partial standalone
integrations (5.2%). melonDS is not counted as integrated yet.
