# NES.emu controller boundary

Pinned upstream source: `Rakashazi/emu-ex-plus-alpha@1c12fac5ce49badaadff2e2f210dcc30b89f4943`.

The pinned README names Android and Linux builds, and `NES.emu/src/main/AppMeta.cc` plus `src/main/options.cc` establish a `NesEmu.config` file and serialized session input-port options. No local Linux package/runtime or verified path/device fixture is available for this catalog entry, so the adapter refuses to synthesize a writer. Android preference/storage and Java input-device integration require a native Android implementation and runtime verification; the cataloged save path is documentation only.
