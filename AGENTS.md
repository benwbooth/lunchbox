# AGENTS

- After completing any requested change, commit the relevant files and push the current branch before finishing.
- If the worktree contains unrelated local changes, stage and commit only the files relevant to your work unless the user says otherwise.
